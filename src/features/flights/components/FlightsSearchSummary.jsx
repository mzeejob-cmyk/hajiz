import { DirectionText } from "../../../design-system/primitives/DirectionText.jsx"
export function FlightsSearchSummary({ query, onEdit }) { const passengers = (query.adults ?? Number(query.travelers || 1)) + (query.children ?? 0) + (query.infants ?? 0); return <section className="flights-summary"><div><div className="summary-route" dir="ltr"><DirectionText>{query.origin ?? query.from}</DirectionText><span>→</span><DirectionText>{query.destination ?? query.to}</DirectionText></div><p><DirectionText>{query.departureDate ?? query.departure}</DirectionText>{query.returnDate && <> <span>—</span> <DirectionText>{query.returnDate}</DirectionText></>}</p><small>{query.tripType === "round_trip" || query.tripType === "round" ? "ذهاب وعودة" : "ذهاب فقط"} · {passengers} مسافر · {query.cabinClass ?? "economy"} · <DirectionText>{query.customerCurrency ?? query.currency ?? "AED"}</DirectionText></small></div><button type="button" onClick={onEdit}>تعديل<span className="desktop-summary"> البحث</span></button></section> }
