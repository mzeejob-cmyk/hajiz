import { useState } from "react"
const CHIPS = ["الأفضل", "الأرخص", "الأسرع", "مباشر"]
export function FlightsResultsToolbar({ onFilters }) { const [selected, setSelected] = useState("الأفضل"); return <div className="results-toolbar"><div className="results-count"><strong>48 نتيجة</strong><span className="mobile-results-label">48 رحلة متاحة</span><label>الترتيب <select aria-label="ترتيب النتائج"><option>موصى به</option></select></label><button className="mobile-filter-trigger" type="button" onClick={onFilters}>الفلاتر</button></div><div className="result-chips" aria-label="عرض النتائج">{CHIPS.map(chip => <button type="button" key={chip} className={selected === chip ? "is-active" : ""} aria-pressed={selected === chip} onClick={() => setSelected(chip)}>{chip}</button>)}</div></div> }
