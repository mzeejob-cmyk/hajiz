import { DirectionText } from "../../../design-system/primitives/DirectionText.jsx"
import { FlightSegment } from "./FlightSegment.jsx"
import { Price } from "./Price.jsx"
export function FlightOfferCard({ offer, onSelect }) { return <article className="flight-offer-card"><div className="offer-airline"><span className="airline-mark" aria-hidden="true">{offer.airlineCode}</span><div><h2>{offer.airline}</h2><DirectionText>{offer.airlineCode} {offer.flightNumber}</DirectionText></div>{offer.recommended && <span className="ranking-badge" aria-label="خيار موصى به">موصى به</span>}</div><FlightSegment offer={offer}/>{offer.segmentCount > 1 && <div className="connection-strip">{offer.segmentCount} مقاطع سفر</div>}<div className="offer-details"><span>◫ {offer.cabin}</span><span>▣ {offer.baggage}</span><span>↻ {offer.flexibility}</span></div><div className="offer-purchase"><div className="price-stack"><Price amount={offer.sellingAmount} currency={offer.currency}/><small>السعر النهائي للعميل</small><span className="price-lock">السعر قابل للتحديث عند المتابعة</span></div><button type="button" onClick={() => onSelect?.(offer.alternativeId)}>اختيار</button></div></article> }
