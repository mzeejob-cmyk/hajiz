import { DirectionText } from "../../../design-system/primitives/DirectionText.jsx"
import { FlightSegment } from "./FlightSegment.jsx"
import { Price } from "./Price.jsx"
export function FlightOfferCard({ offer, onSelect }) { return <article className="flight-offer-card" data-itinerary={offer.key}><div className="offer-airline"><span className="airline-mark" aria-hidden="true">{offer.airlineCode}</span><div><h2>{offer.airline}</h2><DirectionText>{offer.airlineCode} {offer.flightNumber}</DirectionText></div><span className="ranking-badge">{offer.rankingLabel}</span></div><FlightSegment offer={offer}/>{offer.connection && <div className="connection-strip">{offer.connection}</div>}<div className="offer-details"><span>◫ {offer.cabin}</span><span>▣ {offer.baggage}</span><span>↻ {offer.flexibility}</span></div><div className="offer-purchase"><div className="price-stack"><Price amount={offer.sellingAmount} currency={offer.currency}/><small>{offer.additionalOptionsCount}</small>{offer.expiresAt && <span className="price-lock"><DirectionText>{offer.expiresAt}</DirectionText> السعر محفوظ لمدة</span>}</div><button type="button" onClick={() => onSelect?.(offer.key)}>اختيار</button></div></article> }
